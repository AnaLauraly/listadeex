#include <stdio.h>
#include <stdlib.h>

int main()
{
   float notap, notat, media;

   do{
      printf("informe a nota da prova:");
   scanf("%f", &notap);
   if(notap <0 || notap >10){
   printf("valor invalido tente novamente");
   }
   }while(nota< 0 || notap >10);
   printf("nota: %.2f\n"n notap);
   }

    return 0;
}
